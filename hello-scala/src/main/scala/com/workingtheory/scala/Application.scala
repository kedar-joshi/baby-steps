package com.workingtheory.scala

import org.apache.logging.log4j.{LogManager, Logger}

object Application
{
	private val logger: Logger = LogManager.getLogger(Application.this)

	def main(args: Array[String]): Unit =
	{

	}
}
